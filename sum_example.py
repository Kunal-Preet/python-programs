import math

# Using sum function
numbers = [1, 2, 3, 4, 5]
total_sum = sum(numbers)
print("Sum using sum function:", total_sum)

# Using fsum function
float_numbers = [0.1, 0.2, 0.3]
total_fsum = math.fsum(float_numbers)
print("Sum using fsum function:", total_fsum)
